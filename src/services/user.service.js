import { api } from "../api/api";
import { ENDPOINTS } from "../api/endpoint";

// export const submitKycIndividual = async (payload) => {
//   const response = await api.post(ENDPOINTS.SUBMIT_KYC_INDIVIDUAL, payload);
//   return response.data;
// };
